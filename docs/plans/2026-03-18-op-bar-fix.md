# OP_BAR Trace-Driven Fix Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Prevent trace-driven replay from collapsing all SM80 barrier phases into `bar_id=0`, so the large GEMM trace can replay without the current barrier teardown assertion.

**Architecture:** Modify `OP_BAR` decoding at the trace-driven boundary so barrier identity is preserved earlier in the pipeline. Keep the change as small as possible: use the barrier instruction PC to derive a stable synthetic barrier id, then validate with the same large trace under the existing debug instrumentation.

**Tech Stack:** C++, Accel-Sim trace-driven frontend, shell replay workflow, Git.

---

### Task 1: Save the repair design and plan

**Files:**
- Create: `docs/plans/2026-03-18-op-bar-fix-design.md`
- Create: `docs/plans/2026-03-18-op-bar-fix.md`

**Step 1: Write the design doc**
- Save the chosen repair direction and rationale.

**Step 2: Write the implementation plan**
- Save the concrete tasks, files, and validation steps.

**Step 3: Verify the docs exist**

Run: `ls docs/plans/2026-03-18-op-bar-fix*.md`
Expected: both files listed.

### Task 2: Implement the minimal `OP_BAR` fix

**Files:**
- Modify: `gpu-simulator/trace-driven/trace_driven.cc`

**Step 1: Locate `case OP_BAR`**
- Confirm the current placeholder mapping.

**Step 2: Replace fixed `bar_id = 0` with a stable PC-derived barrier id**
- Keep the mapping bounded to the simulator’s barrier-slot capacity.
- Keep `bar_type = SYNC` and `bar_count = (unsigned)-1` unchanged for this iteration.

**Step 3: Keep the patch minimal and documented**
- Add a short explanatory comment describing why PC-derived ids are being used.

### Task 3: Rebuild and rerun the failing trace

**Files:**
- Reuse trace: `modal/artifacts/fp16-2048x12288x12288-gemm3/raw_traces/kernelslist.g`
- Reuse debug logging path under: `modal/artifacts/fp16-2048x12288x12288-gemm3/`

**Step 1: Rebuild Accel-Sim**

Run the normal rebuild command.
Expected: `accel-sim.out` rebuild succeeds.

**Step 2: Rerun the large trace with combined stdout/stderr logging**

Run: the same replay command with `2>&1 | tee ...`
Expected: barrier debug logs now show multiple barrier ids.

**Step 3: Check whether the previous assertion is gone**
- If the replay still fails, capture the new failure mode.
- If it progresses further, record the new stopping point or final metrics.

### Task 4: Verify impact on small cases

**Files:**
- Reuse one existing small trace, e.g. `modal/artifacts/fp16-512x512x512/traces/kernelslist.g`

**Step 1: Run one small replay sanity check**

Run: one verified small replay case.
Expected: it still completes successfully.

### Task 5: Commit and push

**Files:**
- Modify: `gpu-simulator/trace-driven/trace_driven.cc`
- Create: `docs/plans/2026-03-18-op-bar-fix-design.md`
- Create: `docs/plans/2026-03-18-op-bar-fix.md`

**Step 1: Check git status**

Run: `git status --short`
Expected: intended code/docs changes only, ignoring large artifact logs.

**Step 2: Commit**

```bash
git add gpu-simulator/trace-driven/trace_driven.cc \
        docs/plans/2026-03-18-op-bar-fix-design.md \
        docs/plans/2026-03-18-op-bar-fix.md
git commit -m "fix: preserve barrier identity in trace replay"
```

**Step 3: Push**

```bash
git push
```
